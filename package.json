{
    "scripts": {
        "dev:back": "cd backend && cargo run",
        "dev:front": "cd frontend && npm run build:release && npm run build",
        "dev": "npm run dev:front && npm run dev:back"
    }
}
